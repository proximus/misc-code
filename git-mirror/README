Git mirror will try to duplicate repositories to a mirror.

The first time you run, you need to initialize the repositories and syncronize
them to the mirror:
$ ./git-mirror -i

To only syncronize the repository to the mirror:
$ ./git-mirror
